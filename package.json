{
  "name": "domaine-sandbox",
  "config": {
    "store": "domaine-sandbox"
  },
  "scripts": {
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "format:fix": "prettier --write .",
    "dev": "shopify theme dev --ignore templates/**/*.json config/settings_data.json sections/*.json --store $npm_package_config_store"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@shopify/prettier-plugin-liquid": "^1.5.2",
    "eslint": "^9.12.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.10.0",
    "prettier": "^3.3.3"
  }
}